/**
 * Firebase services barrel export
 * Import Firebase services from this file throughout the app
 *
 * Usage:
 *   import { db } from '@/firebase';
 *   import { db, app } from '@/firebase';
 */
export { app, db, analytics, auth, storage } from './config';